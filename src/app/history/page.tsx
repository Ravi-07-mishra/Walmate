import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function HistoryPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Shopping History</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is a placeholder page for your shopping history.</p>
      </CardContent>
    </Card>
  );
}
